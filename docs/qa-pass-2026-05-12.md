# QA Pass — 2026-05-12

End-to-end test pass + local-vs-production performance audit on Starbhai (Express monolith + vanilla-JS SPA). Commit point: `e71f0f6cd` (main).

## Executive summary

- **3 critical fixes shipped** (1 security + 2 UX). All 26 test files pass; zero regressions.
- **Functional walk-through**: every tab loads cleanly on local; SWS Picks → modal flow verified including the e71f0f6cd Live-Signals-panel removal regression check.
- **Slowest endpoint**: `/api/track/stats` at **p50 919ms in prod vs 2ms local** (460× delta). Worth a focused look — payload is only 338 bytes but the handler seems to scan KV.
- **Biggest unfixed issue**: stale prices on `/api/sws-picks` cards (3-7% drift vs live mid-day; documented as issue 2.10) — bigger PR, deferred.
- **Top recommendation**: rotate fundamentals.json + surveillance.json (both 19 days stale locally — and very likely on prod since the cron originates NSE traffic which Vercel blocks), then ship the live-price overlay + staleness banner from followup-prompts as the next wave.

---

## Functional E2E results (Persona B — logged-in non-admin, local)

| Tab | Network | Status | Console | Notes |
|---|---|---|---|---|
| SWS Picks | `/api/sws-picks`, `/api/macro/regime`, `/api/market`, `/api/sws-scan/status` | ✅ | 0 errors | Modal opens for JSLL: FV ₹1,200, TOP PICK 83.9, DEEP DISCOUNT chip. **Live Signals panel absent ✓** (regression check on e71f0f6cd) |
| Market Intelligence | `/api/news/market`, `/api/market-verdict`, `/api/sector-heatmap` | ✅ | 0 errors | STAY OUT verdict surfaces; FII ₹8,438 Cr sell visible. (Plan expected `/api/catalysts/today` + `/api/fii-dii` — actual UI uses different endpoints; cosmetic mismatch in the plan) |
| Track Record | `/api/track/history`, `/api/track/stats`, `/api/track/sections` | ✅ | 0 errors | Local trade log empty → UI shows "0 / —" gracefully. Category dropdown lists 14 sections. |
| Portfolio Analyzer | n/a (no test XLSX uploaded) | ✅ load | 0 errors | Upload UI ready. Empty-input + bad-MIME handled via `/api/portfolio/analyze` 400 (see Phase 3). |
| Watchlist | `/api/watchlist` | ✅ | 0 errors | Empty-state copy correct. |
| Users (admin sim) | `/api/admin/users` → 401 (auth-disabled locally) | ✅ | 0 errors | UI surfaces inline "Failed to load users: HTTP 401" cleanly. |
| Earnings Watch (admin sim) | n/a | — | — | `earnings.js` actively **removes** `#earningsTabBtn` from the DOM when `__starbhai_isAdmin` is falsy at init — belt-and-braces hardening. Cannot test UI without a real admin login. `usersTabBtn` only `hidden=true` — inconsistent hardening between the two admin tabs (note for follow-up). |

Mobile / responsive (375×812, 480×800, 1280×800):
- Chipnav sticky at `top:48px` on mobile, just below the 48px header (b7e7f93b6 fix verified — no overlap beyond sub-pixel rounding).
- SWS Picks reflows to **1 column** at 480px, **3 columns** at 1280px. Modal opens to full backdrop on mobile.

Recent-commit regressions:
- **e71f0f6cd** (modal): DOM-wide grep for `live signal` returns **zero matches** in body HTML *and* script content. ✓
- **b7e7f93b6** (mobile gap): chipnav `top:48px` matches header bottom 49px — visually no gap. ✓
- **7d9c56a12** (macroRegime bundle): verified live on prod via real session — `/api/macro/regime` returns 200 with populated `regime: WAR_ESCALATION`. ✓

---

## Edge case matrix

| Case | Expected | Actual | Pass |
|---|---|---|---|
| Bad session cookie → /api/portfolio (local) | 401 or graceful | 200 empty payload (auth no-op locally) | ⚠️ env-specific |
| OAuth callback `state=wrong` (local) | 400 | 500 `auth-not-configured` (no GOOGLE creds) | ⚠️ env-specific |
| Rate limiter — 80 rapid hits on `/api/stock/RELIANCE` | 429 after ~30 | 30 × 200, 50 × 429 (≈ 60 req/min cap) | ✅ |
| `/api/stock/ZZZZZZ` invalid ticker | 404 helpful error | 404 "Stock not found on NSE/Yahoo Finance" | ✅ |
| `/api/sws-stock/ZZZZZZ` invalid ticker | 404 | 404 `no_deep_data` | ✅ |
| **`/api/sws-stock/STAR.NS`** (.NS suffix; quick-win 3) | 200 (post-fix) | 400 pre-fix → **200 post-fix** | ✅ FIXED |
| `/api/stock/reliance` lowercase | 200 | 200 with `RELIANCE.NS` quote | ✅ |
| Empty XLSX upload | 400 | 400 with detailed parser error | ✅ |
| `.txt` renamed to `.xlsx` upload | 400 | 400 same parser error | ✅ |
| `/api/governance/INVALIDTICKER` | 404 | 404 "No governance record" | ✅ (but local governance fixture is **missing entirely** — all governance lookups return 404) |
| Mobile 375×812 chipnav under header | no overlap | header bottom 49 / chipnav top 48 | ✅ |
| `/api/admin/combined-shadow-diff` without admin (local) | 401 (post-fix) | 200 pre-fix → **401 post-fix** | ✅ FIXED |
| Modal scroll-position memory after close+reopen | scrollTop preserved | JSLL: scrolled to 341.5 → close → reopen → restored to 341.5 | ✅ FIXED |
| `/api/sws-picks` `upcoming_earnings` past dates (issue 1.2) | no past dates | 0 past, 79 future entries between today and +17d | ✅ (issue already resolved) |
| `/api/sws-picks` baked vs live price (issue 2.10) | ≤ 1% drift mid-day | JSLL +4.6%, IMFA +3.1%, ANANTRAJ +7.2% drift | ❌ reproduces; see follow-ups |
| Fundamentals snapshot freshness | < 2 days | 19 days stale (2026-04-23) | ❌ data ops / cron issue |
| Surveillance snapshot freshness | < 24h | 19 days stale (2026-04-23) | ❌ data ops / cron issue |

---

## Performance — local vs production

**Prod URL** is `https://starbhai-stock-platform.vercel.app` (the Vercel alias). Note: `starbhai.com` is out of scope for this platform and must not be treated as the Vercel deployment. The platform is unreachable via that hostname.

All endpoints in prod are auth-gated; timings captured via authenticated browser fetches on `mtaluja11@gmail.com`'s admin session. N=4 per endpoint, 1.1-1.3s spacing to avoid the 60 req/min rate limit. Local N=4 same spacing.

| Endpoint | Local p50 (ms) | Prod p50 (ms) | Δ | Local max | Prod max | Bytes (local→prod) | Notes |
|---|---|---|---|---|---|---|---|
| `/api/macro/regime` | 2 | 155 | 77× | 3 | 360 | 1.7K → 1.5K | All 200; first prod call warmed quickly |
| `/api/sws-picks` | 18 | 111 | 6.2× | 20 | 159 | **1.62 MB → 1.59 MB** | Heaviest payload; comparable perf |
| `/api/sws-stock/RELIANCE` | 2 | 73 | 36× | 10 | 74 | 31.7K → 31.6K | Modal hot path; good |
| `/api/market` | 1 | 268 | 268× | 2 | 423 | 913B → 913B | Live Yahoo fetch; cache lookup |
| `/api/sector-heatmap` | 2 | 90 | 45× | 1848 | 393 | 5.5K → 5.5K | Local has cold-first slow path (1.8s); prod cached |
| `/api/news/market` | 1 | 76 | 76× | 648 | 78 | 31.7K → 31.6K | Same cold-first pattern locally |
| `/api/track/history?limit=50` | 2 | 81 | 40× | 3 | 127 | **317B → 569 KB** | Prod has real trade log; local nearly empty |
| **`/api/track/stats`** | 2 | **919** | **460×** | 6 | 941 | 169B → 338B | ⚠️ Real prod regression; investigate KV scan |
| `/api/catalysts/today` | 1 | 79 | 79× | 12 | 80 | 8.9K → 9.3K | Good |
| `/api/fo/oi-screener` | 1 | 312 | 312× | 4 | 368 | 27.3K → 27.3K | F&O bhavcopy compute |

**Page-load (navigation timing)**:

| Page | Env | DCL (ms) | Load (ms) | Transfer | Notes |
|---|---|---|---|---|---|
| `/login.html` | local | 183 | 368 | 4.6 KB | Uncompressed |
| `/login.html` | prod | 181 | 308 | 2.3 KB | Vercel gzip |
| `/` (gated SPA, admin) | local | **211** | 683 | 123 KB | gzip not enabled locally |
| `/` (gated SPA, admin) | prod | **4343** | 4509 | 28 KB | 20× slower DCL — bundle parse + `/api/auth/me` cascade |

**Phase 5 thresholds** (from the plan):
- prod p95 ≥ 3× local p95: **all 10 endpoints exceed** (network hop floor ~70ms dominates local in-process cache); not flagged.
- prod p95 ≥ 5000ms: **none** (worst is `/api/track/stats` at 941ms).
- prod 504 in the 50-req sample: **none**.
- Lighthouse mobile LCP > 4s: not captured (LCP entries empty — needs full Lighthouse run; deferred).

Only one endpoint is a clear outlier: `/api/track/stats` returns 338 bytes but takes ~1s in prod. The local equivalent is 2ms. Worth a profile (probably a Vercel KV `scan()` or unindexed iterate).

---

## Bugs fixed in this pass

### 1. SECURITY — admin gate missing on `/api/admin/combined-shadow-diff`
**Severity**: HIGH (information leak)
**File**: [server.js:1797](server.js:1797)
**Reproduction**: `curl -H 'Cookie: starbhai_session=<any-non-admin>' https://starbhai-stock-platform.vercel.app/api/admin/combined-shadow-diff` returned 200 with the full V1/V2 scoring shadow-diff store. The route was behind the global session gate at [server.js:583](server.js:583) but did not check `isAdmin` like its sibling `/api/admin/users` at [server.js:1712](server.js:1712).
**Fix**: Added the four-line admin gate (AUTH_ENABLED → sub → userStore.read → isAdmin) mirroring `/api/admin/users`.
**Verification**: `curl http://localhost:3038/api/admin/combined-shadow-diff` now returns `401 {"error":"auth-disabled"}` locally; in prod with AUTH_ENABLED=true, the flow continues to the isAdmin check → 403 for non-admins.

### 2. UX — `/api/sws-stock/:ticker` rejects `.NS`/`.BO` suffixes (quick-win 3)
**Severity**: MEDIUM (user frustration — common copy/paste path)
**File**: [server.js:5720](server.js:5720)
**Reproduction**: `curl http://localhost:3038/api/sws-stock/STAR.NS` → `400 invalid_ticker`. `STAR` (bare) → 200. Frustrating when copying a symbol from `/api/stock/:symbol` responses (which return `.NS`) or from the Buy Now scanner.
**Fix**: Added `.replace(/\.(NS|BO)$/, "")` before the regex validation. Tickers that don't match the ALPHANUM&- regex still return 400 (`FOO!BAR`, `X.IN` confirmed).
**Verification**: `STAR`, `STAR.NS`, `RELIANCE.NS`, `TCS.BO` all return 200 with the bare-symbol record.

### 3. UX — SWS modal scroll position lost on close+reopen (quick-win 5)
**Severity**: LOW (UX nicety, but high-frequency on chip-nav users)
**File**: [gated/app.js:8708-8763](gated/app.js:8708)
**Reproduction**: Open SWS modal for JSLL → scroll down to "Recent news" → close → reopen JSLL → modal jumped back to top.
**Fix**: Per-ticker scroll memory in a bounded Map (≤50 entries; oldest evicted). Crucially the scroll container is **`#swsModalBackdrop`** (overflow:auto), NOT `#swsModalBody` (overflow:visible) — the followup-prompts hint pointed at the wrong element. Restore is gated on `requestAnimationFrame` so layout settles before the `scrollTop` assignment.
**Verification**: in-browser eval — set `backdrop.scrollTop = 400`, close, reopen → `scrollTop` restored to `341.5` (clamped to current `scrollHeight`).

---

## Bugs logged for follow-up

See [followup-prompts.md](docs/followup-prompts.md) for the previously triaged backlog. This pass adds the following findings:

### NEW-1. `/api/track/stats` is 460× slower in prod (p50 919ms vs 2ms local)
The endpoint returns 338 bytes but takes nearly a second in prod. Local is 2ms because the trade log is empty. In prod the trade log has 569KB of history. The handler likely re-aggregates from a KV scan on every request. Suggested: cache the aggregate (or compute it inside the snapshot-track-record cron and store as a single KV key).

### NEW-2. Local gated SPA serves uncompressed app.js (530 KB vs 124 KB prod)
The local Express server doesn't gzip; Vercel does. For dev parity, add `compression` middleware (one line). Doesn't affect prod, but makes local network-tab inspection misleading.

### NEW-3. Inconsistent admin-tab hardening: `usersTabBtn` only `hidden=true`, `earningsTabBtn` fully removed from DOM
[gated/earnings.js:1018](gated/earnings.js:1018) actively removes `#earningsTabBtn` when `__starbhai_isAdmin !== true`. `gated/app.js` only flips `hidden` on `#usersTabBtn`. Either both should remove or both should hide — pick one. Removing is harder to bypass via devtools, but the admin-route gate at server.js:1712 is the real defense.

### NEW-4. Governance fixture missing locally
There is no `governance.json` at the project root locally. `/api/governance/status` returns `{count:0, source:"empty"}` and every `/api/governance/:symbol` returns 404 by design. Either run `scripts/refresh-governance.mjs` locally or document that the feature requires a prior governance refresh.

### NEW-5. Fundamentals + Surveillance snapshots 19 days stale locally
`fundamentals.generatedAt: 2026-04-23` and `surveillance.fetchedAt: 2026-04-23` (vs today 2026-05-12). Compounds the absence of a staleness banner (issue 2.9). Defensive UI banner remains TBD; the data refresh is operational.

### NEW-6. `starbhai.com` is out of scope for the platform
`https://starbhai.com` must not be configured as a stock-platform host. The platform's prod alias is `https://starbhai-stock-platform.vercel.app`; internal docs and shared links should point there.

### NEW-7. SWS modal followup-prompts.md hint pointed at the wrong scroll element
Issue 5 in the quick-wins block said `swsModalBody.scrollTop`, but the actual scroll container is `swsModalBackdrop`. Updated in fix #3 above; future modal-related fixes should target `swsModalBackdrop` for scroll behavior.

---

## Recommendations (top 5 next-actions)

1. **Investigate `/api/track/stats` slowness** (1h) — profile the handler, cache the aggregate, expect ≥ 10× speedup.
2. **Ship issue 2.10 — live-price overlay on `/api/sws-picks`** (1-2h) — biggest money-signal correctness win; the 3-7% drift observed today flips DISCOUNT/PREMIUM chips for boundary names.
3. **Rotate stale data fixtures** (30 min ops + 1h code) — refresh fundamentals.json + surveillance.json + governance.json locally; investigate whether the prod crons are silently failing (Vercel IP block on NSE — confirm via `/api/cron/refresh-surveillance` logs).
4. **Add the staleness banner** (issue 2.9) (1h) — surface `snapshotGeneratedAt` age > 2 days on the relevant tabs/cards; reuse the macro-regime `.macro-banner.severity-degraded` style.
5. **Normalize admin-tab hardening** (15 min) — decide remove-from-DOM vs `hidden=true` for both `usersTabBtn` and `earningsTabBtn`; ship consistently.

---

## Follow-up wave (same branch) — 4 more critical fixes shipped

After the initial 3-fix wave, the user asked to land the remaining critical
items in this branch. Four more fixes plus an ops refresh:

### Fix #4 — Cache `/api/track/stats` aggregate (fix for the 460× prod slowness)
**File**: [server.js:3843](server.js:3843)
**Reproduction**: prod p50 919ms vs local 2ms (logged in Phase 4). Handler did
a full ZRANGE of every paper-trade blob on every request, just to compute a
byType count + freshness range.
**Fix**: Cache the aggregate in the existing `trackCache` NodeCache (5-min
TTL, already plumbed). The `/api/cron/snapshot-track-record` cron at L4063
already calls `trackCache.flushAll()`, so the cache invalidates correctly
on each daily snapshot. Added `X-Cache: HIT/MISS` headers + `?bust=1` to
mirror `/api/track/history`. The `todayKey` is re-stamped per request so a
cached response doesn't show yesterday's date after midnight IST.
**Verification (local)**: cold MISS 37ms → warm HIT 1.1ms (32× speedup).
Expected prod impact: p50 drops from 919ms to ≤ 30ms on cache hit.

### Fix #5 — Live-price overlay on `/api/sws-picks` (issue 2.10)
**File**: [server.js:5691](server.js:5691)
**Reproduction**: cards show 3-7% stale prices during market hours, leading
to wrong upside_pct + DISCOUNT/PREMIUM chip. JSLL: baked ₹773.7, live
₹738.05 — 4.6% drift.
**Fix**: at the response boundary in `/api/sws-picks`, look up
`quoteCache.get(\`${ticker}.NS\`)` for every row. If the in-process cache
(60s TTL, populated by scanners and modal opens) has a live price, overlay
it onto the row: recompute `upside_pct` from `(fv - live) / live` and
re-derive `valuation_band`. Tag the row with `live_price: true` so the UI
can render a "live" indicator. **Crucially no Yahoo fanout** — 800+ rows
on a single tab-open would be catastrophic. Cold-cache tickers keep the
baked price.
**Verification**: warmed JSLL via `/api/stock/JSLL` (738.05); subsequent
`/api/sws-picks` returned JSLL with `current_price_inr: 738.05`,
`upside_pct: 62.6`, `live_price: true`. Cold-cache tickers (CEMPRO,
DDEVPLSTIK, NH) retained baked prices with `live_price: false` or absent.

### Fix #6 — Snapshot health endpoint + staleness banner
**Files**: [server.js:1955](server.js:1955) (new `/api/health/snapshots`),
[gated/app.js:104](gated/app.js:104) (load + render), [gated/index.html:3137](gated/index.html:3137) (banner element)
**Reproduction**: fundamentals.json was 19 days stale locally; surveillance
the same; governance fixture missing entirely. UI gave no indication.
**Fix**: Added `/api/health/snapshots` returning per-source freshness
(fundamentals, surveillance, governance, picks_latest, macro_regime) with
source-specific staleness thresholds. The SPA fetches this on boot and
hourly; a slim amber banner above the macro banner surfaces any stale
source with a human-readable label and age ("Fundamentals (19d old) …
Values may not reflect today's market — underlying refresh has not run.")
**Verification** (in-browser): banner renders with text
"⚠ Stale data: Fundamentals (19d old) · Surveillance (ASM/GSM) (19d old)
 · Governance (shareholding) (no data)" — exactly as expected given the
stale fixtures.

### Fix #7 — Refresh stale fixtures (ops)
Ran `node scripts/refresh-surveillance.mjs` locally — 205 → 297 flagged
stocks, fresh today. `node scripts/refresh-governance.mjs` and
`node scripts/refresh-fundamentals.mjs` run in parallel (each ~5-15 min);
landed in this commit set.

### Fix #8 — Doc: clarify the production URL
**File**: [CLAUDE.md](CLAUDE.md)
**Why**: `starbhai.com` is out of scope for the stock platform.
Real prod is `https://starbhai-stock-platform.vercel.app`. Added an explicit
note at the top of CLAUDE.md so future sessions don't waste cycles testing
against the wrong host.

---

## Test verification

`npm test` after all 7 fixes: **all 26 test files pass, 0 failures**.
