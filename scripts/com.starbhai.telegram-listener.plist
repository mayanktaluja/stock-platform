<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.starbhai.telegram-listener</string>

    <!--
      Phase-3 persistent Telegram-channel listener (fast-news-alerts). Watches
      the channels in data/alerts/channels.json for watchlist-ticker mentions
      and pushes Telegram. Lowest-latency source (desks post here first).

      KeepAlive = { SuccessfulExit: false }: restart ONLY on a nonzero exit
      (crash / lost connection). A clean exit 0 means the listener is dormant
      (no TG_SESSION / no channels configured) and must NOT be respawned, or it
      would hot-loop. Once TG_SESSION + channels are set, kickstart it:
        launchctl kickstart -k gui/$(id -u)/com.starbhai.telegram-listener

      ThrottleInterval spaces out crash-restarts so a persistent failure can't
      spin the CPU.
    -->
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/mayanktaluja/code/stock-platform/scripts/telegram-listener.sh</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
    </dict>

    <key>ThrottleInterval</key>
    <integer>60</integer>

    <key>WorkingDirectory</key>
    <string>/Users/mayanktaluja/code/stock-platform</string>

    <key>StandardOutPath</key>
    <string>/Users/mayanktaluja/code/stock-platform/data/tg-listener-launchd-stdout.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/mayanktaluja/code/stock-platform/data/tg-listener-launchd-stderr.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>HOME</key>
        <string>/Users/mayanktaluja</string>
        <key>LANG</key>
        <string>en_US.UTF-8</string>
    </dict>

    <key>ProcessType</key>
    <string>Background</string>
</dict>
</plist>
